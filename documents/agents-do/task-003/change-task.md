# Báo cáo thay đổi tài liệu Task-003

- Đã cung cấp giải pháp khắc phục triệt để lỗi "Invalid hook call" cho người dùng, sử dụng tham số cấu hình `resolve.dedupe` của Vite.
- Không có thay đổi đáng kể nào ở các tài liệu public (`README.md`, `usage.md`), vì mục `LOCAL_INSTALL.md` tạo ở Task-002 đã nhắc tới mẹo xử lý "Invalid hook call" này. Do đó, việc cấu hình tại project local (`vn-gis-windify-gis-test/windify-gis-react`) là hành động tự thân đủ để xử lý case cụ thể của người dùng.
